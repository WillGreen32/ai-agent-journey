# Day 3 — Token Management & Cost Control

This mini-toolkit helps you **count tokens**, **estimate cost**, and **chunk long text** before making API calls.  
Works **offline** — no API key required.

---

## Quickstart (Windows PowerShell)

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install tiktoken
